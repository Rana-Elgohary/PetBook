import { UserUpdateDetails } from './user-update-details';

describe('UserUpdateDetails', () => {
  it('should create an instance', () => {
    expect(new UserUpdateDetails()).toBeTruthy();
  });
});
